<div class="right">
  <%= link_to 'Cancel', @user, class: button_classes('warning') %>
  <%= link_to 'user Index', users_path, class: button_classes %>
</div> <!-- right -->
<h1>Editing User</h1>

<%= render 'form', user: @user %>

<div class="right">
  <%= link_to 'Delete User', @user, method: :delete, data: {confirm: "Are you sure?"}, class: button_classes('danger') %>
</div>
